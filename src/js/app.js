/* eslint-disable no-console */
import $, { ajax, noop } from './jlight';

$(() => {
  const $button = $('button');
  const $divs = $('div');
  const $spans = $('.span');
  const $input = $('input');
  const $link = $('a');

  // Try something
});
