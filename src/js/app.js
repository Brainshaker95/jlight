/* eslint-disable no-console */
/* eslint-disable no-unused-vars */
import $, {
  ajax,
  get,
  post,
  noop,
  uuid,
  preventEvent,
  dashCaseToCamelCase,
  camelCaseToDashCase,
} from './jlight';

$(() => {
  const $button = $('button');
  const $divs = $('div');
  const $spans = $('.span');
  const $input = $('input');
  const $link = $('a');

  // Try something
});
