/* eslint-disable no-console */
/* eslint-disable no-unused-vars */
import $ from './index';

$(() => {
  const $button = $('button');
  const $divs = $('div');
  const $spans = $('.span');
  const $input = $('input');
  const $link = $('a');

  window.$ = $;

  // Try something
});
